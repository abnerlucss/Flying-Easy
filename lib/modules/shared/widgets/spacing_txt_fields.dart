import 'package:flutter/material.dart';

class SpacingTextFormFields extends StatelessWidget {
  const SpacingTextFormFields({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 20,
    );
  }
}
